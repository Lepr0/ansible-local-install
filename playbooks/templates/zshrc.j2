export ZSH="/home/{{ ansible_env.USER }}/.oh-my-zsh"

export ZSH="$HOME/.oh-my-zsh"


ZSH_THEME="robbyrussell"

plugins=(
zsh-navigation-tools
{% for plugin in zsh_custom_plugins %}
  {{ plugin.name }}
{% endfor %}
git
bundler
dotenv
macos
rake
rbenv
ruby
)

source $ZSH/oh-my-zsh.sh


alias ll="ls -la"


for possiblekey in ${HOME}/.ssh/*_rsa; do
  if grep -q PRIVATE "$possiblekey"; then
    ssh-add "$possiblekey" 2>/dev/null
      fi
      done
cd ~
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH
alias vpn='/home/greem/Apps/Outline-Client.AppImage'
alias ide='/home/greem/IDEA/IntelliJIDEA2024.1\(UltimateEdition\)/bin/idea.sh'
___MY_VMOPTIONS_SHELL_FILE="${HOME}/.jetbrains.vmoptions.sh"; if [ -f "${___MY_VMOPTIONS_SHELL_FILE}" ]; then . "${___MY_VMOPTIONS_SHELL_FILE}"; fi