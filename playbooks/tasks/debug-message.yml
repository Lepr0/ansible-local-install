- name: debug
  block:
    - name: "DEBUG: Read after install"
      ansible.builtin.debug:
        msg:
        - "Do not forget:"
        - "~/.aws dir"
        - "~/.kube dir"
        - "~/.ssh dir"
        - "~/.config/yandex-disk dir"
        - "~/.gitconfig file"
        - "~/.zsh_history file"
        - "awsexport in ~/.zshrc"
        - "Please REBOOT your laptop"
